# Data

- `raw/` — untouched data as pulled from external sources (API-Football, StatsBomb, SkillCorner).
- `processed/` — cleaned, normalized, ML-ready data produced by the pipeline.
- `external/` — reference/lookup data not produced by our pipeline.

Do not commit large or raw datasets to git. See root `.gitignore`.
